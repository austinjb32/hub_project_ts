schema: .\src\modules\users\users.graphql
generates:
  # Specify where our generated types should live.
  ./__generated__/resolvers-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
      # More on this below!
     # contextType: "../index#MyContext